# Extensions: Exercise 01 Hex Dump

Possible follow-up work:

- Support stdin as input.
- Add configurable row width.
- Add optional ASCII column.
- Add colorized non-printable byte display behind a flag.
- Add golden-output integration tests.
- Benchmark whole-file loading against streaming.
- Compare the implementation with `xxd` or `hexdump` behavior for selected
  cases.
